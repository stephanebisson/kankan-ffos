GOAL: separate component from usage


- cleanup, profiling, optimization
- transition based on velocity OR offset
- infinite scrolling (with only 3 panes in DOM)
- make a reusable component
- options for no header OR externalize header from component

- kankan interface with hardcoded data file
